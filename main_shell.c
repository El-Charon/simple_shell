#include "shell.h"

/**
 * main - Entry point for the shell program.
 *
 * Calls the run_shell function and returns 0.
 *
 * Return: Always 0.
 */
int main(void)
{
    run_shell();
    return (0);
}